module.exports = function (source) {
    // console.log(a,b, c,d, this.getOptions());
    // console.log();
    // source = source.replace(/console\..*/g, '');
    // return source;
    this.callback(null, source, null, 2323)
}